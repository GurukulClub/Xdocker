;$document.ready({
	
	//Call method to get top 5 recent requisitions, deployments and comments
	// look through each array to render a dic for recent req, deployment and comments
	$.getJson(SITE_URL + '/admin/requisitions/getDashboardData')
	
	
	
})

